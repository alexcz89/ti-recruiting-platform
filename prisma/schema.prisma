// =========================================================
// Prisma Schema
// =========================================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// =========================
enum Role {
  ADMIN
  RECRUITER
  CANDIDATE
}

enum EmploymentType {
  FULL_TIME
  PART_TIME
  CONTRACT
  INTERNSHIP
}

// 👇 NUEVO: seniority para vacantes
enum Seniority {
  JUNIOR
  MID
  SENIOR
  LEAD
}

enum ApplicationStatus {
  SUBMITTED
  REVIEWING
  INTERVIEW
  OFFER
  HIRED
  REJECTED
}

// 👇 Interés del reclutador
enum ApplicationInterest {
  REVIEW
  MAYBE
  ACCEPTED
  REJECTED
}

// 👇 Estatus de la vacante
enum JobStatus {
  OPEN
  CLOSED
  PAUSED
}

// Catálogo: tipo de término
enum TaxonomyKind {
  SKILL
  CERTIFICATION
  LANGUAGE
}

// Nivel de idiomas
enum LanguageProficiency {
  NATIVE
  PROFESSIONAL
  CONVERSATIONAL
  BASIC
}

// =========================
// Escolaridad (para candidatos y minDegree en Job)
// =========================
enum EducationLevel {
  NONE
  PRIMARY
  SECONDARY
  HIGH_SCHOOL
  TECHNICAL
  BACHELOR
  MASTER
  DOCTORATE
  OTHER
}

enum EducationStatus {
  ONGOING
  COMPLETED
  INCOMPLETE
}

// =========================
// Ubicación de la vacante
// =========================
enum LocationType {
  REMOTE
  HYBRID
  ONSITE
}

// =========================
// Plan de facturación de la empresa (para límites de vacantes)
// =========================
enum BillingPlan {
  FREE
  PRO
  BUSINESS
  AGENCY
}

// =========================
// CFDI / Facturas
// =========================

enum InvoiceStatus {
  PENDING      // Creada en tu sistema, aún sin timbrar
  ISSUED       // Timbrada correctamente
  CANCELED     // Cancelada en el SAT
  ERROR        // Error al timbrar o al comunicarse con el PAC
}

model Invoice {
  id        String   @id @default(cuid())

  // Relación con la empresa que está usando Bolsa TI
  companyId String
  company   Company  @relation(fields: [companyId], references: [id])

  // Datos del receptor (tu cliente)
  customerName  String
  customerRfc   String   @db.VarChar(13)
  customerEmail String?

  // Campos CFDI típicos
  cfdiUse       String?  @db.VarChar(5)  // p.ej. "G03"
  paymentMethod String?  @db.VarChar(3)  // p.ej. "PUE"
  paymentForm   String?  @db.VarChar(3)  // p.ej. "03" transferencia
  currency      String   @default("MXN")

  // Importes
  subtotal Decimal @db.Decimal(10, 2)
  tax      Decimal @db.Decimal(10, 2)
  total    Decimal @db.Decimal(10, 2)

  // Relación con Factura.com / CFDI
  externalId String?      // id de factura.com (si lo maneja)
  uuid       String? @unique // UUID del CFDI una vez timbrado
  series     String?
  folio      String?

  // Enlaces a archivos (puedes guardarlos en S3 luego)
  pdfUrl String?
  xmlUrl String?

  status    InvoiceStatus @default(PENDING)
  createdAt DateTime      @default(now())
  issuedAt  DateTime?
  updatedAt DateTime      @updatedAt

  @@index([companyId, status])
  @@index([uuid])
}

// =========================
model Company {
  id      String  @id @default(cuid())
  name    String
  country String?
  city    String?
  domain  String?
  size    String?
  logoUrl String?
  logoKey String?

  // Plan de facturación actual (lo usa /api/jobs con config/plans.ts)
  billingPlan BillingPlan @default(FREE)

  // ✅ Datos fiscales para CFDI (facturación)
  taxLegalName    String?       // Razón social
  taxRfc          String? @db.VarChar(13)
  taxRegime       String? @db.VarChar(3)   // Código de régimen (ej. "601", "612", etc.)
  taxZip          String? @db.VarChar(5)   // C.P. domicilio fiscal
  taxAddressLine1 String?                 // Calle y número
  taxAddressLine2 String?                 // Colonia / interior / detalles
  taxEmail        String?                 // Correo para envío de CFDI
  cfdiUseDefault  String? @db.VarChar(5)  // Uso CFDI preferido (ej. "G03")
  taxDataCompleted Boolean @default(false)

  users     User[]
  jobs      Job[]
  // 👇 back-relation para plantillas
  templates JobTemplate[] @relation("CompanyTemplates")

  // 👇 back-relation con facturas CFDI
  invoices  Invoice[]

  createdAt DateTime @default(now())
}

// =========================
model User {
  id           String   @id @default(cuid())
  name         String?
  firstName    String?
  middleName   String?
  lastName     String?
  email        String   @unique
  passwordHash String?
  role         Role     @default(CANDIDATE)
  createdAt    DateTime @default(now())

  // ✅ nuevo (requerido por tu flujo de verificación)
  emailVerified DateTime?

  applications Application[]
  jobs         Job[]         @relation("PostedJobs")

  company   Company? @relation(fields: [companyId], references: [id])
  companyId String?

  // EXISTENTES
  location    String?
  locationLat Float?
  locationLng Float?

  // Nuevos para filtros de ubicación
  country    String? @db.VarChar(2)
  admin1     String?
  city       String?
  cityNorm   String?
  admin1Norm String?

  // Opcionales (proveedor de geodatos)
  placeId     String?
  placeSource String?

  phone     String?
  birthdate DateTime?
  linkedin  String?
  github    String?
  resumeUrl String?

  // Skills y certificaciones simples
  skills         String[] @default([])
  certifications String[] @default([])

  // Relaciones detalladas
  candidateSkills      CandidateSkill[]
  candidateCredentials CandidateCredential[]
  candidateLanguages   CandidateLanguage[]

  recruiterProfile RecruiterProfile?

  // Historial laboral
  experiences WorkExperience[]

  // Escolaridad
  highestEducationLevel EducationLevel @default(NONE)
  education             Education[]

  // 👇 back-relation para plantillas creadas por el usuario
  createdTemplates JobTemplate[] @relation("UserCreatedTemplates")

  // 👇 NUEVO: versiones de CV guardadas
  resumes Resume[]

  @@index([locationLat, locationLng])
  @@index([country])
  @@index([country, admin1])
  @@index([country, city])
  @@index([cityNorm])
  @@index([admin1Norm])
}

// =========================
model WorkExperience {
  id        String    @id @default(cuid())
  userId    String
  role      String
  company   String
  startDate DateTime
  endDate   DateTime?
  isCurrent Boolean   @default(false)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, startDate])
  @@index([userId, isCurrent])
}

// =========================
// Educación (lista)
// =========================
model Education {
  id     String @id @default(cuid())
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  level  EducationLevel?
  status EducationStatus

  institution String
  program     String?
  country     String? @db.VarChar(2)
  city        String?

  startDate   DateTime?
  endDate     DateTime?
  grade       String?
  description String?

  // Orden manual para drag & drop
  sortIndex Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId, status])
  @@index([userId, sortIndex])
  @@index([userId, endDate])
}

// =========================
// Vacantes
// =========================
model Job {
  id    String @id @default(cuid())
  title String

  // Slug para URL pública (opcional)
  slug String? @unique

  // Requerido (multiempresa)
  companyId String
  company   Company @relation(fields: [companyId], references: [id])

  // Ubicación y tipo
  location     String
  locationLat  Float?
  locationLng  Float?
  locationType LocationType @default(ONSITE)

  // Filtros por ubicación (estructurado)
  country    String? @db.VarChar(2)
  admin1     String?
  city       String?
  cityNorm   String?
  admin1Norm String?

  // Publicación
  employmentType EmploymentType
  seniority      Seniority?       // 👈 NUEVO (lo usa tu seed.ts)
  description    String   @db.Text
  schedule       String?  // ej. "L-V 9:00–18:00"
  publishAt      DateTime? // publicación programada

  // Lista simple y relación catálogo
  skills         String[]
  requiredSkills JobRequiredSkill[]

  // Compensación
  salaryMin  Int?
  salaryMax  Int?
  currency   String? @default("MXN")
  showSalary Boolean @default(false)

  // Remoto (compatibilidad). Preferir locationType
  remote Boolean @default(false)

  // Prestaciones y banderas de visibilidad
  benefitsJson Json?
  showBenefits Boolean @default(true)

  // Educación / Skills / Certs (payloads del wizard)
  educationJson Json?
  minDegree     EducationLevel?
  skillsJson    Json?
  certsJson     Json?

  // Privacidad
  companyConfidential Boolean @default(false)

  // Estatus de la vacante
  status JobStatus @default(OPEN)

  // Auditoría
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Reclutador que publicó
  recruiterId String?
  recruiter   User?   @relation("PostedJobs", fields: [recruiterId], references: [id])

  // Relaciones
  applications Application[]

  @@index([companyId])
  @@index([title])
  @@index([location])
  @@index([locationLat, locationLng])
  @@index([country])
  @@index([country, admin1])
  @@index([country, city])
  @@index([cityNorm])
  @@index([admin1Norm])
  @@index([status])
  @@index([employmentType, status])
  @@index([publishAt])
}

// =========================
model Application {
  id          String            @id @default(cuid())
  jobId       String
  candidateId String
  resumeUrl   String?
  coverLetter String?
  status      ApplicationStatus @default(SUBMITTED)
  createdAt   DateTime          @default(now())

  // Lógica de rechazo diferido
  rejectedAt         DateTime?
  rejectionEmailSent Boolean   @default(false)

  // Interés/estado del reclutador
  recruiterInterest ApplicationInterest @default(REVIEW)

  candidate User @relation(fields: [candidateId], references: [id])
  job       Job  @relation(fields: [jobId], references: [id])

  messages Message[]

  @@unique([candidateId, jobId])
  @@index([status, createdAt])
  @@index([status, rejectedAt, rejectionEmailSent])
  @@index([recruiterInterest])
}

// ==========================
// Planes de suscripción (tabla genérica, para cuando quieras Stripe, etc.)
// ==========================

model Plan {
  id                String   @id @default(cuid())
  name              String   // "Gratis", "Pro", etc.
  slug              String   @unique // "free", "pro", "business", "agency"
  monthlyPriceCents Int      // en centavos: 99900 = $999.00
  currency          String   // "MXN"
  maxActiveJobs     Int?     // null = ilimitado
  maxUsers          Int?     // null = ilimitado
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  subscriptions Subscription[]
}

model Subscription {
  id        String @id @default(cuid())
  accountId String // o companyId, según tu modelo futuro
  planId    String
  plan      Plan   @relation(fields: [planId], references: [id])

  status           SubscriptionStatus @default(ACTIVE)
  startedAt        DateTime           @default(now())
  currentPeriodEnd DateTime
  canceledAt       DateTime?

  stripeCustomerId     String?
  stripeSubscriptionId String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum SubscriptionStatus {
  ACTIVE
  PAST_DUE
  CANCELED
  TRIALING
}

// =========================
// Plantillas de vacante (para “Usar vacante anterior”)
// =========================
model JobTemplate {
  id String @id @default(cuid())

  companyId String
  company   Company @relation(fields: [companyId], references: [id], name: "CompanyTemplates")

  title   String
  payload Json // snapshot del wizard (todos los campos)

  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  lastUsedAt DateTime?

  // (Opcional) quién la creó
  creatorId String?
  creator   User?   @relation(fields: [creatorId], references: [id], name: "UserCreatedTemplates")

  @@index([companyId, updatedAt])
  @@index([lastUsedAt])
}

// =========================
model CodexEntry {
  id        String   @id @default(cuid())
  title     String
  slug      String   @unique
  excerpt   String?
  content   String   @db.Text
  tags      String[]
  tech      String?
  published Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([published, createdAt])
}

// =========================
enum RecruiterStatus {
  PENDING
  APPROVED
  REJECTED
}

model RecruiterProfile {
  id        String          @id @default(cuid())
  userId    String          @unique
  company   String
  website   String?
  phone     String?
  status    RecruiterStatus @default(PENDING)
  createdAt DateTime        @default(now())
  updatedAt DateTime        @updatedAt

  user User @relation(fields: [userId], references: [id])
}

// =========================
model Message {
  id            String   @id @default(cuid())
  applicationId String
  fromUserId    String
  toUserId      String
  body          String
  createdAt     DateTime @default(now())

  application Application @relation(fields: [applicationId], references: [id])
}

// Auditoría genérica
model AuditLog {
  id        String   @id @default(cuid())
  actorId   String?
  action    String
  target    String?
  meta      Json?
  createdAt DateTime @default(now())
}

// =========================================================
// Catálogo editable de skills / certificaciones / idiomas
// =========================================================
model TaxonomyTerm {
  id          String       @id @default(cuid())
  kind        TaxonomyKind
  slug        String
  label       String
  aliases     String[]     @default([])
  description String?
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  requiredByJobs  JobRequiredSkill[]
  candidateSkills CandidateSkill[]
  candidateCreds  CandidateCredential[]
  candidateLangs  CandidateLanguage[]

  @@unique([kind, slug])
  @@index([kind])
}

// =========================
model JobRequiredSkill {
  id     String  @id @default(cuid())
  jobId  String
  termId String
  must   Boolean @default(true)
  weight Int?

  job  Job          @relation(fields: [jobId], references: [id])
  term TaxonomyTerm @relation(fields: [termId], references: [id])

  @@unique([jobId, termId])
  @@index([termId])
}

// =========================
model CandidateSkill {
  id     String @id @default(cuid())
  userId String
  termId String
  level  Int?
  years  Int?

  user User         @relation(fields: [userId], references: [id])
  term TaxonomyTerm @relation(fields: [termId], references: [id])

  @@unique([userId, termId])
  @@index([termId])
}

// =========================
model CandidateLanguage {
  id     String              @id @default(cuid())
  userId String
  termId String
  level  LanguageProficiency

  user User         @relation(fields: [userId], references: [id])
  term TaxonomyTerm @relation(fields: [termId], references: [id])

  @@unique([userId, termId])
  @@index([termId])
}

// =========================
model CandidateCredential {
  id           String    @id @default(cuid())
  userId       String
  termId       String
  issuer       String?
  issuedAt     DateTime?
  expiresAt    DateTime?
  credentialId String?
  url          String?

  user User         @relation(fields: [userId], references: [id])
  term TaxonomyTerm @relation(fields: [termId], references: [id])

  @@index([termId])
  @@index([userId, termId])
}

// =========================
// NUEVO: Versiones de CV (para snapshots y PDF)
// =========================
model Resume {
  id           String   @id @default(cuid())
  userId       String
  title        String   @default("Mi CV")
  data         Json
  htmlSnapshot String   @db.Text
  createdAt    DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt])
}
