# Copia este archivo a .env y ajusta valores
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ti_recruiting?schema=public"

# NextAuth (AUTH_SECRET también se usa para firmar tokens de verificación de email)
AUTH_SECRET="set-a-random-secret"
NEXTAUTH_URL="http://localhost:3000"

# Email verification (Resend)
RESEND_API_KEY="re_..." # Tu API key de Resend (resend.com)
EMAIL_FROM="Bolsa TI <no-reply@bolsati.mx>" # Remitente de correos de verificación

# App URL (para enlaces en emails)
NEXT_PUBLIC_APP_URL="http://localhost:3000" # Cambiar en producción
